_format_version: "3.0"
services:
  - name: financial-api
    url: http://financialservices-api:8080/
    routes:
      - name: financial-api-route
        paths:
          - /financial
        strip_path: true
        preserve_host: false
        methods:
          - GET
          - POST
          - PUT
          - DELETE
    plugins:
      - name: rate-limiting
        config:
          minute: 20
          policy: local
      - name: prometheus
        enabled: true
        config:
          status_code_metrics: true
          latency_metrics: true
          bandwidth_metrics: true
          upstream_health_metrics: true
          per_consumer: false
